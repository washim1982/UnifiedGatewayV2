<?xml version="1.0" encoding="utf-8"?>
<!-- IIS in-process hosting for the Unified LLM Gateway (ASP.NET Core Module V2).
     Requires the .NET 8 Hosting Bundle on the server. Overlaid onto the publish
     output by publish.ps1. Secrets (Gateway:Security:AdminApiKey, AWS role, etc.)
     are supplied via appsettings.Production.json or app-pool environment, not here. -->
<configuration>
  <location path="." inheritInChildApplications="false">
    <system.webServer>
      <handlers>
        <add name="aspNetCore" path="*" verb="*" modules="AspNetCoreModuleV2" resourceType="Unspecified" />
      </handlers>
      <aspNetCore processPath="dotnet"
                  arguments=".\UnifiedGatewayV2.dll"
                  hostingModel="inprocess"
                  stdoutLogEnabled="false"
                  stdoutLogFile=".\logs\stdout">
        <environmentVariables>
          <environmentVariable name="ASPNETCORE_ENVIRONMENT" value="Production" />
        </environmentVariables>
      </aspNetCore>
    </system.webServer>
  </location>
</configuration>
